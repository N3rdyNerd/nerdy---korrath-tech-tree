# Korath Tech Tree v0.5 (modular, no build tools)

**How to use**

1. Create these files and folders on GitHub Pages exactly as laid out here.
2. Ensure your Supabase project has the `campaigns`, `players`, and `group_state` tables + RLS policies.
3. Open the site → **Host (GM)** or **Join (Player)**.
4. Deep link format: `?campaign=<UUID>&code=<PLAYER_CODE>`.

Supabase URL & Anon Key are hardcoded in `js/supa.js`.
